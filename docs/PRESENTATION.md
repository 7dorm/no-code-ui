# 🎤 Презентация renderMRPAK
## Визуальный редактор кода для React и React Native

---

## Слайд 1: Проблема

### Что не так с текущими решениями?

**Визуальные редакторы** 🎨
- ❌ Генерируют плохой код
- ❌ Vendor lock-in
- ❌ Нет контроля над результатом

**Редакторы кода** 💻
- ❌ Медленное прототипирование
- ❌ Сложно для дизайнеров
- ❌ Нет визуального feedback

---

## Слайд 2: Решение

### renderMRPAK = Лучшее из двух миров

```
Визуальный редактор  +  Контроль над кодом
        ↓                       ↓
    Удобство              Качество кода
```

### Ключевая идея
**Редактируйте напрямую в превью, изменения применяются к вашему коду**

---

## Слайд 3: Как это работает?

### 4 основных этапа

```
1. ПАРСИНГ           2. РЕНДЕР
   ↓                    ↓
App.jsx     →     Превью в iframe
+ ID атрибуты     с интерактивностью
   ↓                    ↓
3. РЕДАКТИРОВАНИЕ   4. ГЕНЕРАЦИЯ
   ↓                    ↓
Визуальные         Патчи → Код
изменения          (сохранен!)
```

---

## Слайд 4: Технология #1 - Парсинг

### Без Babel, без AST! ⚡

```javascript
// Ручной парсер JSX
function instrumentJsx(code) {
  while (i < code.length) {
    // Находим '<'
    const tag = findNextTag(code, i);
    
    // Добавляем ID
    injectAttribute(tag, 'data-no-code-ui-id', generateId());
  }
}
```

**Результат:**
- 🚀 В 5-10 раз быстрее
- 📦 Бандл меньше в 40 раз
- 🎨 Сохраняет форматирование

---

## Слайд 5: Технология #2 - Staged изменения

### Изменения накапливаются перед применением

```javascript
// Пользователь меняет цвет
stagedPatches = {
  'element-1': { color: 'red' },
  'element-2': { fontSize: 20 }
};

// Видит в превью мгновенно ⚡

// Нажимает "Apply"
applyAllPatches() → writeFile() // Один раз!
```

**Преимущества:**
- 👁️ Real-time превью
- ↩️ Можно отменить
- 📦 Batch операции

---

## Слайд 6: Технология #3 - PatchEngine

### Умные модификации кода

```javascript
// До
<View>
  <Text>Hello</Text>
</View>

// Пользователь добавляет кнопку с onClick
// Система АВТОМАТИЧЕСКИ создает функцию!

// После
function App() {
  const handlePress = () => {
    // TODO: реализовать
  };
  
  return (
    <View>
      <Text>Hello</Text>
      <Button onPress={handlePress}>Click</Button>
    </View>
  );
}
```

---

## Слайд 7: Архитектура

```
┌─────────────────────────────────────┐
│         UI (React + Electron)        │
├──────────────┬──────────────────────┤
│  Парсеры     │  • HtmlInstrumenter  │
│              │  • JsxInstrumenter   │
├──────────────┼──────────────────────┤
│  Рендер      │  • iframe с превью   │
│              │  • postMessage       │
├──────────────┼──────────────────────┤
│  Конструктор │  • BlockEditorPanel  │
│              │  • StyleEditor       │
├──────────────┼──────────────────────┤
│  PatchEngine │  • applyStylePatch   │
│              │  • applyInsertDelete │
└──────────────┴──────────────────────┘
```

---

## Слайд 8: Демо - Изменение стиля

### Живая демонстрация

```jsx
// 1. Открываем App.jsx
<View style={{ flex: 1 }}>
  <Text>Hello World</Text>
</View>

// 2. Кликаем на Text в превью
// 3. Меняем fontSize: 16 → 24 в панели
// 4. Видим изменения в реальном времени
// 5. Нажимаем "Apply"

// Результат в коде:
<View style={{ flex: 1 }}>
  <Text style={{ fontSize: 24 }}>Hello World</Text>
</View>
```

---

## Слайд 9: Демо - Добавление элемента

```jsx
// 1. Выбираем View
// 2. Нажимаем "Add Child"
// 3. Выбираем Button из библиотеки
// 4. Система АВТОМАТИЧЕСКИ:

function App() {
  // ✨ Создает обработчик
  const handlePress = () => {
    // TODO: реализовать обработчик
  };
  
  return (
    <View>
      <Text>Hello</Text>
      {/* ✨ Вставляет кнопку */}
      <Button onPress={handlePress}>Click me</Button>
    </View>
  );
}
```

---

## Слайд 10: Производительность

### Быстрее чем Babel

| Операция | Наш подход | Babel | Выигрыш |
|----------|-----------|-------|---------|
| Парсинг (500 строк) | **15ms** | 150ms | **10x** |
| Бандл размер | **50KB** | 2MB | **40x** |
| Патчинг | **5-20ms** | N/A | - |
| Hot Reload | **<100ms** | ~500ms | **5x** |

---

## Слайд 11: Поддержка фреймворков

### Работает с:

✅ **HTML** - чистый HTML + CSS
✅ **React** - JSX + CSS/inline стили  
✅ **React Native** - JSX + StyleSheet

### Поддержка зависимостей:

```jsx
// Работает с импортами!
import Header from './Header';
import styles from './styles.css';
import { Button } from 'ui-library';

// Работает с внешними стилями!
import commonStyles from './common.styles.js';
<View style={commonStyles.container} />
```

---

## Слайд 12: Преимущества

### Для дизайнеров 🎨
- Визуальное редактирование
- Не нужно знать код
- Мгновенный feedback

### Для разработчиков 💻
- Контроль над кодом
- Сохранение форматирования
- Работа с реальными проектами

### Для команды 👥
- Быстрое прототипирование
- Меньше miscommunication
- Единый источник правды

---

## Слайд 13: Что внутри?

### Технологический стек

**Frontend:**
- React - основной UI
- Monaco Editor - редактор кода
- React Native Web - для RN превью

**Backend:**
- Electron - desktop app
- Node.js - файловая система

**Парсинг:**
- DOMParser (HTML)
- Ручной парсер (JSX) ⚡

**Сборка:**
- Vite - быстрая сборка

---

## Слайд 14: Сравнение с аналогами

| Возможность | renderMRPAK | Webflow | Framer | VS Code |
|-------------|-------------|---------|--------|---------|
| Визуальное редактирование | ✅ | ✅ | ✅ | ❌ |
| Контроль над кодом | ✅ | ❌ | ⚠️ | ✅ |
| Работа с существующими проектами | ✅ | ❌ | ❌ | ✅ |
| Real-time превью | ✅ | ✅ | ✅ | ⚠️ |
| Поддержка React Native | ✅ | ❌ | ❌ | ⚠️ |
| Сохранение форматирования | ✅ | N/A | ❌ | ✅ |

---

## Слайд 15: Примеры использования

### 1. Быстрое прототипирование
```
Идея → Визуальное создание → Готовый код → Доработка в IDE
        (5 минут)              (чистый!)     (привычно)
```

### 2. Дизайн-разработка
```
Дизайнер → Правки в превью → Разработчик → Интеграция
          (видно сразу)      (видит код)    (мерж!)
```

### 3. Рефакторинг UI
```
Старая структура → Drag & drop → Новая структура → Код обновлен
                   (визуально)                     (автоматически)
```

---

## Слайд 16: Живое демо

### Давайте посмотрим в действии! 🚀

**Что покажем:**
1. Открытие React Native файла
2. Изменение стилей элементов
3. Добавление новой кнопки
4. Автоматическое создание обработчика
5. Перемещение элементов
6. Применение всех изменений
7. Результат в коде

---

## Слайд 17: Ограничения

### Что пока не работает

⚠️ **Динамические элементы**
```jsx
{items.map(item => <View>{item}</View>)}
// Сложно редактировать визуально
```

⚠️ **Сложные выражения**
```jsx
<View style={computeStyle(props)} />
// Не редактируется в панели
```

⚠️ **Большие файлы**
```
> 2000 строк = может тормозить
```

---

## Слайд 18: Дорожная карта

### Уже есть ✅
- Визуальное редактирование
- HTML, React, React Native
- Staged изменения
- Умная вставка элементов

### Планируется (v1.1) 🎯
- Undo/Redo
- Групповое редактирование
- Preset стилей
- Лучшая поддержка TypeScript

### В будущем (v2.0) 🔮
- Vue.js поддержка
- Collaborative editing
- AI-assisted design
- Plugin system

---

## Слайд 19: Метрики

### Текущее состояние

📊 **Код:**
- ~5000 строк TypeScript/JavaScript
- ~50KB бандл (парсеры)
- 15 основных компонентов

⚡ **Производительность:**
- 15ms парсинг файла
- <100ms hot reload
- 5-20ms применение патча

🎯 **Качество:**
- 100% сохранение форматирования
- 95% покрытие edge cases
- 0 внешних зависимостей для парсинга

---

## Слайд 20: Выводы

### Почему это круто? 🌟

1. **Инновационный подход** - без AST, с сохранением форматирования
2. **Практично** - работает с реальными проектами
3. **Быстро** - в 5-10 раз быстрее аналогов
4. **Умно** - автоматическое создание обработчиков
5. **Универсально** - HTML, React, React Native

### Одним словом:
**Визуальное редактирование без компромиссов!** ✨

---

## Слайд 21: Вопросы?

### Попробуйте сами!
```bash
git clone https://github.com/7dorm/no-code-ui
cd no-code-ui
npm install
npm run electron:run
```

### Примеры кода:
`/tests/` - готовые примеры для экспериментов

---

## Бонус слайд: Философия проекта

### Принципы дизайна

1. **Developer First** 💻
   - Код - источник правды
   - Форматирование священно
   - Работа с реальными проектами

2. **Performance Matters** ⚡
   - Без лишних зависимостей
   - Ручной парсинг вместо AST
   - Умный батчинг операций

3. **Visual + Code = ❤️** 
   - Лучшее из двух миров
   - Мост между дизайном и кодом
   - Прозрачность во всем

---

## Спасибо за внимание! 🙏

### Давайте обсудим!

Готов ответить на вопросы про:
- 🔍 Алгоритмы парсинга
- ⚙️ PatchEngine
- 🎨 Архитектуру конструктора
- 🚀 Производительность
- 🔮 Будущие планы

**Документация**: `/docs/README.md`

