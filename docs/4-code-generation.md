# ⚙️ Генерация кода: От визуальных изменений обратно к коду

## 🎯 Главная идея

**Превращаем визуальные изменения из конструктора обратно в исходный код, сохраняя форматирование и читаемость**

Пользователь кликает, тянет, меняет → система запоминает → нажатие Apply → код магически обновляется. Всё это без AST, через умные текстовые манипуляции.

---

## 🎨 Типы изменений: 5 основных операций

```
📊 Стили           → Изменение colors, размеров, отступов
➕ Вставка         → Добавление новых элементов
➖ Удаление        → Удаление существующих элементов  
🔄 Перемещение     → Изменение иерархии (drag & drop)
📝 Текст           → Редактирование текстового содержимого
```

Каждая операция = отдельный "патч" к коду.

---

## 🏗️ PatchEngine: Мозг операций

### **Архитектура**

```
                PatchEngine
                (координатор)
                     ↓
        ┌────────────┼────────────┐
        ↓            ↓            ↓
   HTML патчеры  JSX патчеры  Утилиты
   • Стили       • Стили      • Поиск
   • Атрибуты    • Вставка    • Замена
                 • Удаление   • Валидация
                 • Reparent
                 • Текст
```

**Принцип**: Каждый тип файла (HTML/JSX) имеет свои патчеры, но интерфейс единый.

💡 **Польза**: Можно добавить новый тип файла без переписывания всей логики.

---

## 🎨 Операция 1: Изменение стилей

### **Три варианта, три подхода**

#### **Вариант A: Inline стили** (HTML/React)

**HTML:**
```html
<!-- До -->
<div style="color: red">

<!-- Патч: { fontSize: '20px' } -->

<!-- После -->
<div style="color: red; font-size: 20px">
```

**React:**
```jsx
// До
<View style={{ flex: 1 }}>

// Патч: { backgroundColor: 'red', padding: 20 }

// После
<View style={{ flex: 1, backgroundColor: "red", padding: 20 }}>
```

**Технология**: Парсим объект стилей → добавляем/обновляем свойства → сериализуем обратно.

💡 **Польза**: Простой случай, работает быстро и надежно.

---

#### **Вариант B: StyleSheet в том же файле** (React Native)

```jsx
// До
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white'
  }
});

<View style={styles.container}>

// Патч для 'container': { padding: 20 }

// После
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
    padding: 20  // ← добавлено!
  }
});
```

**Технология**:
1. Находим `StyleSheet.create(...)`
2. Ищем нужный ключ (`container`)
3. Находим его объект `{...}`
4. Добавляем/обновляем свойства

💡 **Польза**: Стили остаются в StyleSheet, как и должно быть в RN.

---

#### **Вариант C: Внешний файл стилей** (React Native)

```jsx
// App.jsx
import commonStyles from './styles/common.js';
<View style={commonStyles.container}>

// styles/common.js (до)
export default StyleSheet.create({
  container: {
    flex: 1
  }
});

// Патч: { padding: 20 }

// styles/common.js (после)
export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 20  // ← добавлено в ДРУГОМ файле!
  }
});
```

**Технология**:
1. Система понимает: `style={commonStyles.container}` → внешний стиль
2. Находит путь к файлу через импорты
3. Загружает внешний файл
4. Применяет патч там
5. Сохраняет внешний файл
6. (Опционально) Меняет ссылку если создали новый стиль

💡 **Польза**: Работает с любой структурой проекта, не ломает архитектуру.

---

### **Умная логика выбора**

```
Есть style={{ ... }}?
  ├─ Да → Патчим inline
  └─ Нет → Есть style={styles.foo}?
      ├─ Да → styles локальный?
      │   ├─ Да → Патчим StyleSheet.create в файле
      │   └─ Нет → Патчим внешний файл
      └─ Нет → Создаем новый style={{}}
```

**Система сама решает** как лучше применить патч, основываясь на текущей структуре кода.

---

## ➕ Операция 2: Вставка элементов

### **Два режима: child и sibling**

#### **Child (внутрь)**

```jsx
// До
<View>
  <Text>Hello</Text>
</View>

// Insert child: <Button>Click</Button>

// После
<View>
  <Text>Hello</Text>
  <Button>Click</Button>  // ← внутри View
</View>
```

**Технология**: Находим закрывающий тег `</View>` → вставляем перед ним.

---

#### **Sibling (рядом)**

```jsx
// До
<View>
  <Text>Hello</Text>
</View>

// Insert sibling: <Button>Click</Button>

// После
<View>
  <Text>Hello</Text>
</View>
<Button>Click</Button>  // ← рядом с View
```

**Технология**: Находим конец элемента (после `</View>`) → вставляем после него.

---

### **Умная вставка обработчиков** 🧠

**Проблема**: Пользователь добавляет `<Button onPress={handlePress}>`

Но функции `handlePress` не существует! Код сломается.

**Решение - автоматическое создание:**

```jsx
// До
function App() {
  return (
    <View>
      <Text>Hello</Text>
    </View>
  );
}

// Пользователь добавляет Button с onPress

// После (система сделала ВСЁ автоматически!)
function App() {
  const handlePress = () => {
    // TODO: реализовать обработчик
  };

  return (
    <View>
      <Text>Hello</Text>
      <Button onPress={handlePress}>Click</Button>
    </View>
  );
}
```

**Как работает:**
1. Парсим snippet: есть `onPress={handlePress}`?
2. Ищем функцию `handlePress` в компоненте
3. Нет? → Создаем!
4. Находим компонент (function/arrow/class)
5. Находим `return` в теле компонента
6. Вставляем функцию ПЕРЕД return
7. Вставляем элемент

💡 **Польза**: Никогда не получите сломанный код. Система заботится о деталях.

---

### **Умное определение места вставки**

```jsx
function App() {
  const [count, setCount] = useState(0);
  const data = fetchData();
  
  // ← Тут НЕ вставляем (до хуков)
  
  const handleOld = () => {};
  
  // ← Тут МОЖНО вставить (после хуков, с другими обработчиками)
  
  return (...);  // ← ИЛИ тут (перед return)
}
```

**Система ищет "хорошее" место**:
1. После всех useState/useEffect (не ломаем Rules of Hooks)
2. С другими const/function (группируем логику)
3. Перед return (логично и читаемо)

---

## ➖ Операция 3: Удаление элементов

### **Умное удаление**

```jsx
// До
<View>
  <Header />
  <Content>
    <Text>Hello</Text>
  </Content>
  <Footer />
</View>

// Delete Content (включая детей!)

// После
<View>
  <Header />
  <Footer />
</View>
```

**Технология**:
1. Находим открывающий тег `<Content>`
2. Self-closing? → Удаляем только его
3. Нет? → Ищем парный `</Content>` (с учетом вложенности!)
4. Удаляем всё между ними

**Умная проверка вложенности:**
```jsx
<Content>
  <Content>  // ← Вложенный с тем же именем!
    <Text />
  </Content>
  <Text />
</Content>
```

Система считает depth, чтобы найти **правильный** закрывающий тег.

💡 **Польза**: Не удаляет лишнего, даже с вложенными одноименными тегами.

---

## 🔄 Операция 4: Перемещение (Reparent)

### **Самая сложная операция**

**Задача**: Переместить элемент A внутрь элемента B.

**Проблемы**:
1. Нужно извлечь A (со всеми детьми)
2. Найти место в B
3. Вставить туда A
4. При этом позиции в коде сдвигаются!

**Пример:**
```jsx
// До
<View id="sidebar">
  <Button id="save">Save</Button>
</View>
<View id="header">
  <Text>Title</Text>
</View>

// Перемещаем Button из sidebar в header

// После
<View id="sidebar">
  // пусто
</View>
<View id="header">
  <Text>Title</Text>
  <Button id="save">Save</Button>  // ← переехал!
</View>
```

---

### **Алгоритм перемещения**

```
1. Запомнить позиции source и target
2. Извлечь source элемент (сохранить текст)
3. Удалить source из кода
4. ⚠️ ВАЖНО: Пересчитать позиции (после удаления всё сдвинулось!)
5. Найти место в target (перед </target>)
6. Вставить туда source
7. Готово!
```

**Самое сложное - шаг 4:**
```
Если source был ДО target в файле:
  target позиция -= длина source
  
Если source был ПОСЛЕ target:
  target позиция не меняется
```

💡 **Польза**: Можем реструктурировать UI без копипасты кода.

---

### **Защита от ошибок**

**Нельзя:**
```jsx
// ❌ Переместить элемент в себя
<View id="A">
  <View id="A">  // ← Бесконечная рекурсия!
  </View>
</View>

// ❌ Переместить элемент в своего потомка
<View id="parent">
  <View id="child">
    <View id="parent">  // ← Парадокс!
    </View>
  </View>
</View>
```

**Проверка:**
```
if (source находится внутри target) {
  return error("Нельзя переместить в потомка");
}
```

---

## 📝 Операция 5: Изменение текста

```jsx
// До
<Text>Hello World</Text>

// Set text: 'New Text'

// После
<Text>New Text</Text>
```

**Технология**:
1. Находим открывающий тег `<Text>`
2. Находим закрывающий `</Text>` (с учетом вложенности)
3. Заменяем ВСЁ между ними
4. Готово!

**Edge case - вложенные элементы:**
```jsx
<Text>
  Hello <Text>World</Text>
</Text>

// При изменении внешнего Text, не трогаем внутренний
```

**Решение**: Отслеживаем depth (как при удалении).

---

## 🎯 Батчинг: Применение всех изменений разом

### **Проблема последовательного применения**

```jsx
// Исходный код: 1000 строк

// Применяем патч 1 → позиции сдвинулись
// Применяем патч 2 → позиции сдвинулись ЕЩЁ
// Применяем патч 3 → позиции сдвинулись ОПЯТЬ
// ...
// Применяем патч 20 → карта элементов уже неактуальна!
```

**Решение - правильный порядок:**

```
1. Все УДАЛЕНИЯ (от конца файла к началу)
   ↓
2. Все ВСТАВКИ (от конца файла к началу)
   ↓
3. Все ПЕРЕМЕЩЕНИЯ (сложные, делаем осторожно)
   ↓
4. Все ПАТЧИ СТИЛЕЙ (не меняют позиции других элементов)
   ↓
5. Все ИЗМЕНЕНИЯ ТЕКСТА (не меняют позиции других элементов)
```

**Почему от конца?**
```
Удаляем снизу вверх:
• Удалили строку 100 → строки 1-99 не сдвинулись
• Удалили строку 50 → строки 1-49 не сдвинулись
• Можем использовать исходные позиции!

Удаляем сверху вниз:
• Удалили строку 50 → строки 51-100 сдвинулись
• Нужно пересчитывать все позиции!
```

💡 **Польза**: Максимально используем исходную карту элементов, минимум пересчетов.

---

## 🛡️ Валидация и защита от ошибок

### **Проверки перед применением**

```
✅ Элемент существует в карте?
✅ Позиции валидны (start < end, в пределах файла)?
✅ Патч стилей содержит валидные CSS свойства?
✅ Вставка не создаст невалидный JSX?
✅ Перемещение не создаст цикл?
✅ Удаление не удалит корневой элемент?
```

**Если проверка провалилась:**
```
1. Показываем понятное сообщение пользователю
2. НЕ применяем изменение
3. Логируем ошибку для дебага
4. Предлагаем alternative (если возможно)
```

💡 **Польза**: Невозможно сломать код через UI. Система умнее пользователя.

---

## 🎨 Сохранение форматирования: Главная фишка

### **Традиционный подход (AST)**

```jsx
// До (ваш красивый код)
function App() {
  return (
    <View style={{ 
      flex: 1,
      backgroundColor: 'white' 
    }}>
      <Text>Hello</Text>
    </View>
  );
}

// После применения изменений через AST
function App(){return <View style={{flex:1,backgroundColor:"white",padding:20}}><Text>Hello</Text></View>;}
```

❌ Форматирование потеряно, код нечитаем.

---

### **Наш подход (текстовые манипуляции)**

```jsx
// До
function App() {
  return (
    <View style={{ 
      flex: 1,
      backgroundColor: 'white' 
    }}>
      <Text>Hello</Text>
    </View>
  );
}

// После (добавили padding: 20)
function App() {
  return (
    <View style={{ 
      flex: 1,
      backgroundColor: 'white',
      padding: 20  // ← только это изменилось!
    }}>
      <Text>Hello</Text>
    </View>
  );
}
```

✅ Форматирование сохранено, изменения минимальны, Git diff чистый.

---

### **Как мы это делаем**

**Принцип**: Меняем только то, что реально нужно изменить.

```javascript
// НЕ делаем:
const newCode = generateCodeFromAST(ast);

// Делаем:
const before = code.slice(0, position);
const after = code.slice(position + length);
const newCode = before + change + after;
```

**Преимущества:**
- ✅ Сохраняются пробелы
- ✅ Сохраняются переносы строк
- ✅ Сохраняются комментарии
- ✅ Сохраняется стиль кода

💡 **Польза**: Git diff показывает реальные изменения, не "переписали весь файл".

---

## 📊 Производительность

### **Бенчмарк: Файл 500 строк**

| Операция | Время | Примечание |
|----------|-------|------------|
| **Патч стиля** | ~5ms | Самая быстрая |
| **Вставка элемента** | ~10ms | + создание обработчика |
| **Удаление** | ~5ms | Простой случай |
| **Reparent** | ~15ms | Самая сложная |
| **Изменение текста** | ~5ms | Быстро |
| **Batch (20 операций)** | ~100ms | Всё разом |

### **Оптимизации**

1. **Ленивый поиск** - не ищем элементы, пока они не нужны
2. **Кеширование** - регулярки компилируются один раз
3. **Умный порядок** - минимизируем пересчеты позиций
4. **Одна запись** - writeFile только в конце

---

## 💡 Преимущества подхода

### **Для пользователей** 👥
- ✅ Результат мгновенно виден
- ✅ Можно отменить до применения
- ✅ Понятные сообщения об ошибках

### **Для кода** 💻
- ✅ Форматирование сохранено
- ✅ Комментарии не потеряны
- ✅ Git история чистая
- ✅ Код остается читаемым

### **Для команды** 👥
- ✅ Легко ревьювить PR (минимальный diff)
- ✅ Нет конфликтов с ручными правками
- ✅ Можно продолжить в любом редакторе

---

## 🎯 Реальные сценарии

### **Сценарий 1: Дизайнер меняет цвета**

```
• 20 элементов
• Изменил цвета у всех
• Нажал Apply
• ~100ms → готово
• Git diff: 20 строк изменено
```

**Без staged:**
```
• 20 записей в файл
• 20 перезагрузок превью
• ~2 секунды
• Git история: 20 коммитов
```

---

### **Сценарий 2: Реструктуризация UI**

```
• Перемещение 5 элементов
• Удаление 2 элементов
• Вставка 3 новых
• Apply → ~150ms
• Код переписан, структура изменена
• Всё работает!
```

---

### **Сценарий 3: Рефакторинг стилей**

```
• Было: 10 элементов с inline стилями
• Нужно: Вынести в StyleSheet
• Система: Создала StyleSheet
• Система: Заменила ссылки
• Система: Удалила inline стили
• Apply → готово
```

---

## 🔮 Будущие улучшения

### **Планируется**
- [ ] **Транзакции** - откат при ошибке
- [ ] **Diff preview** - показать изменения до Apply
- [ ] **Auto-formatting** - опциональный Prettier после патча
- [ ] **Conflict resolution** - умное слияние concurrent изменений

### **Исследуется**
- [ ] **Semantic merging** - Git merge с пониманием кода
- [ ] **AI-assisted refactoring** - предложения улучшений
- [ ] **Code smell detection** - предупреждения о проблемах

---

## 🎯 Итоги

### **Почему наш подход к генерации кода особенный:**

1. **Без AST** - текстовые манипуляции быстрее и проще
2. **Форматирование** - сохраняется 100%
3. **Умные автоматизации** - создание обработчиков, проверки
4. **Staged подход** - батчинг операций, валидация перед Apply
5. **Защита от ошибок** - невозможно сломать код через UI

**Главное**: Это не просто "заменить текст в файле". Это умная система с валидацией, автоматизацией и заботой о качестве кода.

**Философия**: Код, сгенерированный инструментом, должен быть таким же чистым и читаемым, как код, написанный человеком.
