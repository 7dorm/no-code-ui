Preact Vite Workspace — full redo

Features:
- Element system: classes extending ElementBase, registry maps kind to renderer
- HTML elements: div, button, img, a, h1-h6
- Preact elements: box, text, card
- Element Library for adding elements
- Draggable elements in workspace (move)
- ProjectHandler saves to localStorage and notifies subscribers
- Export HTML/ZIP (via JSZip CDN)


To run:
npm install
npm run dev
