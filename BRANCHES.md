# Описание веток репозитория

Этот документ описывает содержимое различных веток в репозитории no-code-ui.

## Текущая ветка: `main`

Основная рабочая ветка проекта. Содержит текущую стабильную версию приложения с поддержкой:
- Редактирования HTML, React и React Native файлов
- Блочного редактора с инструментацией кода
- Системы патчей для применения изменений стилей и структуры
- Поддержки различных фреймворков (HTML, React, React Native)

## `origin/OCHIR-BACKEND`

**Статус:** Ветка с backend-реализацией на TypeScript

**Основное содержимое:**
- Полная реализация backend движка для работы с проектами
- TypeScript код в директории `backend/`
- Парсеры для различных форматов:
  - `CssParser.ts` - парсинг CSS
  - `HtmlParser.ts` - парсинг HTML
  - `ReactParser.ts` - парсинг React/JSX
- Система мутаторов для изменения структуры:
  - `addComponent.ts` - добавление компонентов
  - `addElement.ts` - добавление элементов
  - `createElement.ts` - создание элементов
  - `deleteBlock.ts` - удаление блоков
  - `updateCss.ts` - обновление CSS
- Файловые операции:
  - `add.ts` - добавление файлов
  - `createFile.ts` - создание файлов
  - `delete.ts` - удаление файлов
  - `editParams.ts` - редактирование параметров
  - `updateCss.ts` - обновление CSS в файлах
- Основной класс `Engine.ts` (VisualEngine) для управления проектом
- Тесты: `blackbox.test.js`
- Демо файлы: `demo.ts`, `codegen-smoke.ts`

**Отличия от main:**
- Добавлена полная директория `backend/` с TypeScript кодом
- Удалены некоторые файлы документации
- Изменен `.gitignore`
- Добавлен `dependencies.json`

**Последний коммит:** `bc4e13f` - "sexartyomsex"

## `origin/backend`

**Статус:** Более ранняя версия backend-реализации

**Основное содержимое:**
- Аналогичная структура с `OCHIR-BACKEND`, но без некоторых файлов
- Те же парсеры, мутаторы и файловые операции
- Отсутствует `backend/src/engine/Files/updateCss.ts`
- Отсутствует `backend/codegen-smoke.ts`
- Меньше документации (удалены все файлы из `docs/`)

**Отличия от main:**
- Добавлена директория `backend/` с TypeScript кодом
- Удалены все файлы документации из `docs/`
- Изменен `.gitignore`

**Последний коммит:** `ad841ba` - "мутаторы и файлы"

**Примечание:** Похоже, что это более старая версия backend ветки, которая была предшественником `OCHIR-BACKEND`.

## `origin/interaction-with-blocks`

**Статус:** Экспериментальная ветка с полной переработкой архитектуры

**Основное содержимое:**
- Полная переработка на Preact вместо React
- Новая архитектура с системой элементов:
  - Базовый класс `ElementBase`
  - Регистр элементов (registry maps kind to renderer)
  - HTML элементы: div, button, img, a, h1-h6
  - Preact элементы: box, text, card
- Компоненты:
  - `Toolbar` - панель инструментов
  - `Workspace` - рабочее пространство с перетаскиваемыми элементами
  - `ElementLibrary` - библиотека элементов для добавления
- `ProjectHandler` - управление проектом с сохранением в localStorage и системой подписок
- Экспорт HTML/ZIP через JSZip CDN
- TypeScript вместо JavaScript

**Отличия от main:**
- Полностью переписан `src/App.jsx` → `src/App.tsx` на Preact
- Удалены многие компоненты из main ветки:
  - `BlockEditorPanel.jsx`
  - `FileTree.jsx`
  - `RenderFile.jsx`
  - `WebView.jsx`
  - Все файлы из `blockEditor/`
- Удалены Electron файлы (`electron/main.js`, `electron/preload.js`)
- Удалена большая часть документации
- Изменена структура проекта

**Последний коммит:** `af60079` - "22"

**Примечание:** Это экспериментальная ветка с альтернативной архитектурой, использующей Preact и другую систему управления элементами.

## Сравнительная таблица

| Ветка | Технологии | Назначение | Статус |
|-------|-----------|------------|--------|
| `main` | React, Electron | Основная рабочая версия | ✅ Активная |
| `OCHIR-BACKEND` | TypeScript | Backend движок с парсерами и мутаторами | 🔄 Разработка |
| `backend` | TypeScript | Ранняя версия backend | 📦 Архивная |
| `interaction-with-blocks` | Preact, TypeScript | Экспериментальная переработка | 🧪 Экспериментальная |

## Рекомендации

- **Для разработки основной функциональности:** используйте ветку `main`
- **Для работы с backend логикой:** используйте ветку `OCHIR-BACKEND`
- **Для экспериментов с новой архитектурой:** используйте ветку `interaction-with-blocks`

---

*Документ создан: $(date)*
*Последнее обновление: автоматически при изменении веток*

